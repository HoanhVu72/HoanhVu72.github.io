#include <bits/stdc++.h>
using namespace std;
int main(){
    long long a, b, c;
    double ((1 <= a) && (a < 1e9));
    double ((1 <= b) && (b < 1e9));
    double ((1 <= c) && (c < 1e9));
    cin >> a >> b >> c;
    long long S = a*(b+c) + b*(a+c);
    cout << "S = " << S << "\n";
}